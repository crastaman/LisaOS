---
schema: lisaos.gpt_context.v1
last_updated: 2026-07-08
owner: LisaOS
status: placeholder
---

# BAR Technologies Context

## Current state of this file

`identity/IDENTITY.md` lists **BAR** as one of Lisa's primary projects
(alongside WBS/HEB, Lisa Core, and future AI automation systems), but no
detailed BAR Technologies business context — product scope, stakeholders,
priorities, terminology — has been captured anywhere in `~/Lisa` yet. This
file is intentionally a placeholder rather than a fabricated summary:
inventing BAR-specific facts here would silently corrupt every future GPT
Advisor recommendation touching BAR-related jobs.

## Rule for the Advisor

Until this file is populated, treat any BAR-tagged Decision Bundle as
context-limited: rely only on what the bundle itself states, and if BAR
domain knowledge would materially change the recommendation, say so
explicitly in `open_questions` rather than assuming.

## Rule for maintainers

When BAR context is available, replace this section with real content
(structure it the same way as `05_WBS_CONTEXT.md` once populated) and log
the change in `CHANGELOG.md`.
