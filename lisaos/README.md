# LisaOS v2

LisaOS is the WBS orchestration layer built on top of OpenClaw.

OpenClaw is the execution platform.
Lisa is the orchestration agent.
WBS is the primary production workload.

LisaOS does not replace OpenClaw.
LisaOS configures OpenClaw with WBS-specific governance, routing, sprint, QA and engineering policies.
